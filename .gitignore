node_modules/
dist/
coverage/
.env
.env.*
!.env.example
*.log
npm-debug.log*
.DS_Store
*.tsbuildinfo

# crontick runtime data dirs (local dev)
.crontick/
.dev/

# scratch artifacts from scripts/verify-package-install.mjs
.verify-package-scratch/
*.tgz

# ad hoc scratch dumps (e.g. Get-Process snapshots) accidentally left at the
# repo root during manual debugging -- never commit these; use a directory
# outside the repo instead
/*.txt